# Mobile Agent for OpenClaw

让手机成为 OpenClaw 的远程触角，执行移动设备特有操作。

---

## 📱 功能特性

| 功能 | 状态 | 说明 |
|------|------|------|
| 📱 读取短信 | ✅ 可用 | 读取手机收到的短信 |
| 📩 发送短信 | ✅ 可用 | 远程发送短信 |
| 👥 读取通讯录 | ✅ 可用 | 导出联系人 |
| 📍 GPS定位 | ✅ 可用 | 获取精确位置 |
| 🔔 显示通知 | ✅ 可用 | 在手机上弹出通知 |
| 📷 拍照 | ⚠️ 部分 | 需手动确认 |
| 📸 截图 | ⚠️ 部分 | 需额外模块 |
| 🌐 打开网页 | ✅ 可用 | 启动浏览器 |

---

## 🚀 快速开始

### 1. 安装依赖

```bash
cd mobile-agent
npm install
```

### 2. 配置服务器

编辑 `src/config.js` (如需要):
```javascript
export const DEFAULT_SERVER = 'wss://your-server.com';
```

### 3. 运行到手机

**Android:**
```bash
# 连接手机，开启USB调试
adb devices

# 运行
npm run android
```

**iOS:**
```bash
# 需要Mac + Xcode
npm run ios
```

### 4. 编译 APK (发布)

```bash
cd android
./gradlew assembleRelease

# 输出路径:
# android/app/build/outputs/apk/release/app-release.apk
```

---

## 📋 系统要求

- **Android**: 8.0+ (API 26+)
- **Node.js**: 16+
- **React Native CLI**: 已安装

---

## ⚙️ 权限配置

首次运行需要授权：

1. **短信权限** - 读取/发送短信
2. **通讯录权限** - 读取联系人
3. **位置权限** - GPS定位
4. **相机权限** - 拍照
5. **存储权限** - 保存文件

---

## 🔧 常见问题

### Metro bundler 启动失败

```bash
# 清除缓存
npm start -- --reset-cache
```

### Gradle 构建失败

```bash
cd android
./gradlew clean
./gradlew assembleRelease
```

### 应用崩溃

检查是否已授予所有权限：
```bash
设置 → 应用 → Mobile Agent → 权限
```

---

## 🏗️ 项目结构

```
mobile-agent/
├── App.js                      # 主入口
├── package.json
├── android/                    # Android 工程
│   ├── build.gradle
│   └── settings.gradle
├── ios/                        # iOS 工程 (自动生成)
└── src/
    ├── services/
    │   ├── AgentService.js     # WebSocket连接
    │   └── ActionExecutor.js   # 指令执行
    ├── components/
    │   └── LogViewer.js        # 日志组件
    └── config.js               # 配置
```

---

## 🔗 与 OpenClaw 集成

1. 部署 `cross-platform-agent` 服务端
2. 在手机上安装此 App
3. 配置服务器地址
4. 在 OpenClaw 中使用 `/mobile` 指令

---

## 📄 许可证

MIT
