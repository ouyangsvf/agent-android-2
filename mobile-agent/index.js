/**
 * Mobile Agent v3.0 - React Native Entry Point
 * RN 0.76 + New Architecture Ready
 */

import { AppRegistry } from 'react-native';
import App from './App';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => App);
